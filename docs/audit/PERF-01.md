---
id: PERF-01
title: Identity Performance & Scalability Findings Report
stage: 7
gate: G7 Pre-Implementation Performance Architecture Review
status: FINDINGS FRAMED — SPECIFIED pattern only. Budget numbers = UNDEFINED (REQUIRES APPROVED POLICY if no spec numbers exist)
owner: Principal DevOps Architect + Principal Backend Architect
derives_from: [G7-REVIEW-002 §10 + §17, ADR-0001 Runtime Stack TanStack Start + Lovable Cloud capabilities, IDN-DMN-001 INV-18 Primary Membership Serialisation Trade-off, ADR-0002 Football Id lookup performance, IDN-FE-001 Frontend Architecture Performance/Observability/Offline sections (PART 20/21/22 AUD-IDN-2026-002 coverage)]
satisfied_by: G6 Load Test Report + Production Observability Baseline (not Sprint 3 scope)
adrs: [ADR-0001, ADR-0002, ADR-0003]
north_star_impact: Performance failures at P2+ Regional Scale → Guardian timeout retry → consent abandon mid-flow → CTI voluntary opt-in drops → Phase Exit halt. Roster national read scalability (INV-18) failure → Federation cannot process National Championship roster submission on deadline → Operational FAILED, VAP count indirectly drops because ineligible players unable to participate competition (no roster submitted → no A2 match events = ActivityPolicy threshold fail → drop from ACTIVE→TEMPORARILY_INACTIVE)
---

# PERF-01
## Identity Performance & Scalability Findings

16 areas per Sprint 3 §8. Rule: DO NOT invent performance budget numbers. If spec already HAS approved number (e.g. ActivityPolicy rolling_window = 90 days min_events = 2 from PRG-MET-001 APPROVED G0) → trace to source. Otherwise: **UNDEFINED — REQUIRES APPROVED POLICY/ARCHITECTURE DECISION**. No invented 500ms / 2s / 10,000 rpm values. Must use exact approved sources only.

---

## Finding Table (16 Review Areas)

| Finding ID | Performance Area | Requirement / Architecture Pattern | Approved Policy / Budget Exists? (Y/N + Source) | Current Evidence / Spec Pattern | Gap | Architecture Risk | Severity | Owner | Remediation | Verification Method (after budget exists) | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **PERF-01-F01** | Football ID lookup latency: display_code vs football_id — two lookup pathways have different performance because display_code is NOT an FK (ADR-0002 deliberate design: display_code NEVER FK, NEVER auth subject) → display_code lookup has extra index indirection vs football_id direct PK | N (NO latency budget ms approved anywhere — no performance SLA numbers in approved policy yet) · Budget = **UNDEFINED** | ADR-0002: display_code unique index on table but NOT used as FK anywhere (joins always football_id PK); Lookups by display_code rate-limited per SEC-01-F10 (abuse resistance); Q-03 QueryFIDByDisplayCode authenticated; typical flow uses $playerRef (opaque route internal football_id not display_code URL → display lookup happens RARELY: on manual entry FID share card scan, not on navigation) · Pattern SPECIFIED; no numbers | Budget threshold ms undefined Council must approve; Load test plan not defined; Football ID lookup index design OK pattern-wise; Cannot claim budget met because no numbers | Under-estimated → Org admin bulk manual lookups during registration day long queues; Overestimated → infrastructure overspend | **MAJOR** (Phase 0 10 SSB launch day risk — registration window time constrained) | Principal DevOps Principal Data Architect | Council to approve look up SLA p95 p99 display_code PK football_id; Add to PerformancePolicy named versioned config object parallel ActivityPolicy; Index design Postgres VERIFY both indexes present CREATE UNIQUE INDEX (display_code) + PK (football_id); EXPLAIN ANALYZE queries; CACHE strategy: display_code lookup short TTL cache 60s only auth (not long to avoid stale) | (1) Council resolution PerformancePolicy.APPROVED lookup latency ms; (2) Load test 200 concurrent lookups display_code 90s window p95 < approved ms; (3) EXPLAIN shows index scan not seq scan both lookups | SPECIFIED pattern separate indirection; BUDGET UNDEFINED COUNCIL |
| **PERF-01-F02** | Opaque identifier lookup general: All refs in URLs are $playerRef $orgRef not human-readable values → internal resolver maps opaque ref → football_id (ADR-0002 rev.2 route design); This resolver adds ~hop vs direct integer ID route | N (no ms budget approved) · UNDEFINED | IDN-FE-001 §5 route naming rule $param prefix $playerRef NOT display_code/football_id for security (enumeration resistance access log safety); Deep links rule 6 403/404 indistinguishability also requires resolver runs always even for deny cases · Resolver SPECIFIED pattern | Resolver cost unknown; Deny-case overhead same as allow (required for indistinguishability → acceptable trade off security over perf); No resolver cache strategy defined | Opaque resolver overhead at P3+ Provincial scale 2M req/day if inefficient → infrastructure cost + latency | **MINOR** | Principal FE Architect Principal Security Architect (security trade off owner) | Define resolver strategy: signed cookie/session cached playerRef→football_id mapping per user session not global cache (global cache enumeration risk security); Never allow batch resolver endpoint (one ref at a time to stop enumeration); Resolver ms budget defined Phase 2 scale plan | (1) Security test: attempt 1000 $playerRef values batch → 429 rate limit blocks; (2) Load test resolver p95 < approved council ms; (3) Session cached mapping reduce DB hit repeated navigations same player | SPECIFIED route naming pattern; BUDGET UNDEFINED · resolver cache strategy NOT INVENTED (must not risk enumeration) |
| **PERF-01-F03** | Membership Primary serialization (INV-18 Domain Model explicit trade-off): "Serialize national roster read = BAD trade off → explicitly NOT enforce single Active Primary constraint via DB row-level constraint; instead enforce via TransferService domain service + Saga pattern" → At national scale (Phase 3 500k active players) this means potential temporary race 2 concurrent Primary updates (eventual consistency) | Y (Trade-off EXPLICITLY documented IDN-DMN-001 INV-18 → this is APPROVED architecture decision recorded in domain spec; NOT budget numbers but architecture direction YES approved) · Consistency model = **EVENTUAL not STRONG** documented source; Latency budget for national roster reads undefined | INV-18 documented; Domain Service TransferService pattern SPECIFIED; ERD Membership entity primary_flag + active_status; Roster read queries for Federation Championship go against Journey Projection (CQRS read model EDEC-01 not write model) → performance read-heavy optimized; Domain write path uses saga compensating on conflict | Saga pattern spec high level; Retry count/backoff values undefined; Projection rebuild after race undefined; Federation championship critical night-before-event timeline unknown; Cannot quantify how often inconsistency occurs without production data | STRONG consistency requirement unexpected by Federation (e.g. last-minute transfer needs to reflect IMMEDIATELY on competition eligibility check → eventual consistency wrong result 0.01% of time = disqualified player eligible/ineligible mismatch) | **CRITICAL** (INV-18 trade off impacts competition eligibility; requires documented agreed federation process) | Principal Domain Architect (INV-18 Owner) + Principal Football Operations Expert (Council role eligibility rules) · Principal Backend (saga) | Document INV-18 Eligibility Check Window: Federation MUST close transfer window 24 HOURS BEFORE eligibility determination (projection consistent within 24 hour SLA); Council approve 24h consistent window value NOT INVENTED HERE default hours maintained OPEN; Transfer Saga compensation spec: Double-Primary detected → event MEM-05 PrimaryChanged rollback automatic to previous winner based on timestamp; Eligibility API runs ONLY on Projection not write model; Document explicitly "National Championship eligibility = Projection state at Window Close T-24h"; Post-transfer support ticket process defined for 24h window edge cases | (1) Chaos test: inject concurrent 2 Primary transfers different orgs at same ms; Saga outcome = 1 final Active Primary after compensation passes with both actors notified (MEM events correct); (2) Eligibility query T minus 1 hour of close → uses snapshot not live write; (3) Federation agrees window hours SIGNED document | APPROVED architecture trade off INV-18 exists (yes); CONSISTENCY WINDOW HOURS value UNDEFINED COUNCIL |
| **PERF-01-F04** | Journey projection reads: CQRS read model JourneyProjection entity separate; read heavy queries player timeline at scale (10s thousands events per long-career player); projection rebuilt async EventStore events; Projection materialized view strategy pattern unspecified | N (no query ms budget) · UNDEFINED budget; Projection rebuild SLA UNDEFINED (hours / events per minute) | ERD structural #2 EventStore authoritative append only; JourneyProjection 15 entity list; EDEC-01 read-only principle; Events 49 catalogued total · Pattern SPECIFIED projection separate; No strategy materialized view vs table vs Redis timeseries store | Rebuild time after new player (FID issuance) to first READY unknown; Career 15-year 2000 events projection build latency unknown; Pagination strategy on timeline long journeys; Cache long career projection last 90 days summary default; No cache invalidation defined after new event appended | Long career coach opens timeline slow → training attendance recording delayed; Player/Guardian perceives system unresponsive → abandonment → CTI drops | **MAJOR** | Principal Backend Architect (CQRS) | Materialized view strategy (Postgres MATERIALIZED VIEW CONCURRENTLY refresh schedule vs event-driven update after new event); Timeline API: default query last 90 days only unless scroll pagination further; last_90_days materialized separate small; pagination cursors stable not offset; Cache strategy last_90_days TTL 5 minutes short not stale; Event append triggers invalidation immediate not TTL wait | (1) Synthetic 2000 events 15 year career player → timeline API last 90 days p95 < budget ms; (2) Event CON-03 ConsentRevoked appended → cache invalidation next request new data not 5min old; (3) Full history pagination page 2 → correct 10 items no duplicate no skip | SPECIFIED projection pattern; BUDGET ms + materialized strategy + TTL seconds all UNDEFINED (no values invented) |
| **PERF-01-F05** | National roster reads Federation scope: Phase 3 P3 Provincial Coverage → 500K players (per PRG-VIS-001 Phase 3 target matrix → 50 SSB × 1500 avg players approx), Federation GET all roster eligible P6 consent = large result set; streaming/pagination/csv export not invented | Y (Phase targets players total numbers PRG-VIS-001 §10 matrix approved G1 PASSED — numbers exist, QUERY PERFORMANCE BUDGET for national scope still UNDEFINED ms) · Player count = APPROVED numbers from PRG-VIS · Query budget N | PRG-VIS-001 Phase 3 target numbers: Players = large-scale (exact matrix numbers approved); Federation export referenced SEC-01-F07 triple-gate; No export chunked strategy defined; CSV report size limit undefined | 500k player CSV single response → OOM timeout gateway; Federation championship operations staff deadline night before = time critical; Pagination vs streaming vs async job vs email-sent-later pattern not defined | Federation cannot process 500k roster national → operational FAILED → players miss competition registration → A2 match event not recorded → ACTIVE→INACTIVE VAP drop KPI | **CRITICAL** (operational impact KPI downstream) | Principal DevOps Principal Backend Federation Ops Council rep | Async Job pattern: National roster POST submit request → return job_id; Job runs background → produce chunked CSV uploaded storage; Notification sent when ready (S3/Lovable Storage); NOT synchronous 500k row response; Max rows per page API; chunk_size value COUNCIL OWNED not invented | (1) Job submit national 500k → job_status response 202 Accepted not JSON rows; (2) Completion notification sent user; (3) Chunk size rows per file approved value; Export file size limit Council-approved MB threshold | Player count numbers APPROVED (PRG-VIS); chunk size + async job SLA hours UNDEFINED no values |
| **PERF-01-F06** | CQRS query scaling total: 12 queries (Q-01..Q-12) covering all read paths; Query vs Command infrastructure separation: Command synchronous transactional; Query scalable read replicas possible in future Phase 3 scale | N (no QPS approved) · Query QPS budget Phase 0 / Phase 1 / Phase 3 = **UNDEFINED** | IDN-API-001 P-04 strict CQRS 22/12 split; Commands = write side, Queries = read side; ADR-0001 Lovable Cloud server functions: separate route files /api/public/* external vs internal; No replica configuration plan yet defined | Read/write ratio unknown Phase 0 pilot; Read replica timing not planned; Cannot scale to Phase 3 if all queries hit primary write DB (latency + transaction lock contention) | Write lock contention on primary during registration peak Sat/Sun SSB training days → commands timeout during FID issuance peak → new players stuck → never_active inflation guard triggered → VAP counted incorrectly | **MAJOR** | Principal Backend Principal Data Architect | Phase plan: Phase 0 no replica (single instance sufficient for 10SSB 80 coaches concurrent 1500 players); Phase 1 add replica read queries only PROMOTED council decision threshold QPS; Define query routing write primary vs read replica at server function data-access layer; explicit query functions use read replica from Phase 1+; Monitor lock waits in Phase 0 to inform decision | (1) Phase 0 load test 100 concurrent users → command lock waits < approved threshold ms (council threshold undefined); (2) Write attempt on read replica returns error routing guard; (3) Query routing layer configuration correctly maps 12 Q → read 22 C → write; misroute fails tests | SPECIFIED CQRS strict split; NO replica budget QPS thresholds invented |
| **PERF-01-F07** | EventStore append-only read patterns: 49 events × 500k players × 50 events avg career lifetime = 12.5 Billion rows scale Phase 4 National; Event schema version migration forward-only reversible compensation (lifecycle forward-only migrations); Partition strategy per event_type/date not defined | Y (forward-only migration rule quality-gates.md G3-05 Migration forward-only reversible by compensation = APPROVED pattern exists) · Schema rule Y / Volume partition strategy N = **UNDEFINED** per year/event_type | ERD structural commitment #2 EventStore authoritative append-only; Event Catalogue 49 events immutable past tense; G3 migration rule G3-05 "forward-only reversible compensation" (approved quality gates) | Partition strategy not spec; Event ID snowflake/UUID ordering; Index on person_ref, aggregate_id, occurred_at; No hot partition analysis; GDPR/UUDP delete event (Person Right to be Forgotten) vs append-only (append Tombstone event not delete actual rows — approach correct retention) | 12.5B rows no partition → seq scans query by person → seconds query; Audit compliance can't locate event in billion; Retention R5 Permanent means never DROP old partition but archive tier needed | **CRITICAL** (Phase 4 scale impossible without partition strategy defined before G6 not later after production data exists — too late add) | Principal Data Architect (partition strategy) | G3 ERD APPROVED prerequisite: Add Partition Strategy section to ERD IDN-ERD-001 logical appendix; Partition key = occurred_at monthly composite person_id subpartition; Event type secondary index; Cold storage (archive) partition older 5 years to cheaper tier; Tombstone event for Right To Be Forgotten appends instead delete (already compliant since append-only); Migration forward-only approach apply all event versions documented | (1) ERD appendix signed by Principal Data Architect partition strategy described; (2) Simulated 12.5B rows test database partition query person + date range → index only scan hits correct monthly partition; (3) 5-year-old partitions moved to archive storage transparently | Migration pattern APPROVED G3 quality gate rule G3-05; Partition strategy details UNDEFINED for Phase 4 scale |
| **PERF-01-F08** | Projection scalability: New event appended → JourneyProjection + all affected denormalized views (RosterProjection, FederationEligibilityProjection, AnalyticsProjection, CTIProjection) updated asynchronously; fan-out update count per event not measured | N (projection fan-out N events → N×K projections updates budget max ms) · UNDEFINED | EDEC-01 principle read-only projections; 7 read projections implied (Journey, RosterOrg, RosterFed, ActivityVAP, CTI aggregate, NDI graph, JCS completeness) = 7 consumers per 1 domain event; Event bus outbox pattern SPECIFIED general (no specific tech: Lovable Cloud EventBridge equivalent) | Project rebuild lag p95 unknown; Federation eligibility projection not updated within eligibility window → PERF-01-F03 eligibility compounding error; Projection update idempotency strategy not defined (same event processed twice = no double count) | Federation eligibility wrong because projection not refreshed before window close → disqualification | **MAJOR** (combine F03 INV-18 eligibility issue + projection lag = double risk) | Principal Backend (event bus) Principal DevOps (monitoring lag) | Projection lag alert Prometheus metric: projection_last_event_consumed_ts per projection type; lag > threshold MINUTES (council not invented) → alerts to Principal DevOps + Federation Ops; Idempotency key event_id in every projection update handler; Dedup table processed event_ids per projection type | (1) Lag metric dashboard 7 projection values realtime; (2) Inject duplicate event CON-03 event_id=XYZ → projection count same (no double → idempotent); (3) Alert test lag threshold minutes → on-call notification within 1 min | SPECIFIED projection count 7 architecture pattern; Threshold value alert minutes UNDEFINED COUNCIL |
| **PERF-01-F09** | Rate limiting configuration values: (a) display_code lookup rate limit org/min SEC-01-F10; (b) general API auth user/min; (c) Anon IP/min rate limit public endpoints; (d) Export job generation per user/day (prevent abuse) | N ALL sub-items UNDEFINED · 4 rate limit threshold values ALL Council-owned NOT invented | Rate limit concept mentioned ADR-0002 FID lookup; security reasons stated SEC-01; No numeric thresholds approved | 4 required threshold values absent; Implementation MUST NOT hardcode "10/min 50/min 100/min 3/day" DEFAULT; Use policy table values loaded runtime with OPEN fallback to "strict until council decide" placeholder | Placeholder too strict → legitimate user blocked; Too loose → enumeration/abuse succeeds → data breach/performance degradation | **CRITICAL** (combined security+perf) | Principal Security (abuse) Principal DevOps (perf) | Rate limit policy config per 4 categories in policy registry; Server reads at startup not hardcoded; Policy UI for System Admin role to modify post-G6 in PROD; Before council decisions all 4 default to "REJECT_ALL" only for Phase 0 environment (offline pilot); never invent numbers in source code | (1) Grep codebase for hardcoded digits "10/minute" "50/hour" pattern → 0 matches; (2) policy DB records 4 rows all status OPEN_PENDING; (3) Attempt unrecognized API → 429 after tiny rate; approved thresholds populated later after decision → runtime reload | ALL 4 THRESHOLD VALUES UNDEFINED COUNCIL OWNED; no invented number placeholder anywhere |
| **PERF-01-F10** | Caching rules: (a) Which data cacheable; (b) TTL; (c) Never cacheable (consent status realtime always — MUST read actual not cache; guardian verification level realtime on authorization never cache) | Y Partial: Consent Status + Verification Level realtime = NEVER CACHE pattern SPECIFIED (SEC-01 + Auth eval step 4/5 always live); (b) TTL general NONE approved = UNDEFINED for safe data | SPECIFIED non-cacheable: Auth eval step 4 consent active check must hit DB/cache-of-record ONLY not stale 5m cache (risk revocation effective but cached still granted); Verification level same (L1 upgraded L2 should effect immediate not 10m later) · Journey 90 days summary cache short PERF-01-F04 · Football Passport share view cache short TTL share scope duration | TTL values undefined ALL categories; Cache invalidation event list not defined (which domain events invalidate which cache keys) | Wrong cache TTL 1h for consent → revocation takes 1h effective system → High Risk Revocation IMMEDIATE principle broken → Rule 0 violated effect-first flow broken | **BLOCKER** (breaks Rule 0 effect-first revocation architecture) | Principal Security (realtime effect) Principal Backend (cache strategy) | Non-cacheable list hardcoded: "consent.status", "guardian.verification_level", "membership.active_status_primary", "high_risk_revocations_effective" → ALWAYS bypass cache database read only; All other cachable data: event type → cache invalidation trigger mapping defined in document; TTL per data category loaded policy NOT hardcoded | (1) Issue CON-06 HighRiskRevocation → subsequent GET /consents/{ref} within 1 second → shows revoked=true NEVER shows granted (no cache); (2) L1→L2 level upgraded C-19 VerificationCompleted → next request within 500ms API returns level = L2 in C-06 Grant P8 eligibility check; (3) Grep TTL hardcoded "3600" → 0 matches not invented values | Non-cacheable list SPECIFIED CONSENT LEVEL VERIFICATION ALWAYS LIVE good; TTL VALUE SECONDS for ALL OTHER cache categories UNDEFINED COUNCIL |
| **PERF-01-F11** | Cache invalidation rules: CQRS write command success → invalidate corresponding read caches not wait TTL; List of events → cache invalidation keys mapping explicit | N mapping UNDEFINED; TTL fallback undefined | Pattern stated IDN-FE-001 PART 08 CQRS invalidation (PART to be reviewed AUD-IDN-2026-002) · Invalidation pattern concept exists no mapping | 49 events → 7 projections × N cache keys → N×M mapping entries undefined now; Cannot write invalidation code without explicit mapping per command to cache keys | Stale data after update e.g. Transfer → coach still sees old club on roster → wrong training attendance recorded → ActivityPolicy incorrect A1 events → VAP counting mis-attribution | **MAJOR** (KPI counting correctness) | Principal Backend CQRS | Document CACHE_INVALIDATION_MAP.md appendix: C-01 RegisterPerson → inv caches: person_list, org_roster_{orgId}, search_people_index; C-14 PlayerTransferred → inv: person_journey_{playerRef}, org_roster_old, org_roster_new, federation_eligibility_projection, ndi_metrics materialized; Mapping mandatory every 22 command listed explicitly | (1) Automated test for every C-01..C-22: run command, read cache key that should be invalidated → returns cache-miss next access (not stale); (2) Mapping document 22 entries completeness review signoff G4 API | Pattern SPECIFIED in FE architecture PART 08 reference; Explicit 22 commands invalidation key mapping MISSING DOCUMENT (PENDING) |
| **PERF-01-F12** | Regional (P2) → National (P3/P4) scale: Opaque Football ID (ADR-0002) ZERO encoded semantics = NO regional breaking change when cross-region (no "Region code encoded in FID means P2 Sulsel players have ID incompatible with Papua region" scenario avoided ARCHITECTURALLY because opaque ID) | Y (Opaque decision ADR-0002 rev.2 ACCEPTED = architecture direction APPROVED pattern not budget) | ADR-0002 rev2 full opaque (decision encoded after G0 rejected original semantic encoding); display_code no semantical bits; football_id UUID v4 random (no timestamp/node id encoded MAC address version 1 avoided, v4 random ok); Issuing org facts stored separate data columns NOT encoded | No operational performance impact migration risk (zero encoded means no decoder legacy); Still infrastructure scaling horizontal when add regional nodes, but identifier compatibility guaranteed | GREAT NEWS: identifier portability nationally structurally guaranteed; RISK: If someone LATER introduces "regional shard ID" encoded into route params (breaks opaque rule new) → reintroduces breaking portability | **MINOR** (positive mitigation ADR-0002 already handled scale risk structurally) | Chief Enterprise Architect (opaque FID enforcement permanent) | GATE REVIEW AUDIT FOREVER: Any new API/route proposal check pattern identifier regional = NOT violate opaque; Add as standing ADR-0002 compliance check in all future ADR reviews; Audit log in G7 every release check; | (1) Code review check list item "FID Opaque Violation?"; (2) Grep route regex pattern /[A-Z]{2}-[0-9]{4}/ (regional code in URL) → 0 matches routes; (3) Federation cross-region data merge test: 2 regional datasets combined player IDs portable no duplicates | Opaque architecture APPROVED ADR-0002 structural portability guaranteed PERMANENTLY; no budget numbers needed for this finding (architecture decision only) |
| **PERF-01-F13** | PWA / Offline implications: IDN-FE-001 PART 22 PWA Offline section (coverage AUD-IDN-2026-002 deep dive); Service Worker precache routes; IndexedDB store offline actions queue sync when back online; Offline first: attendance recording critical SSB low signal rural training grounds (Phase 0 Sulsel pilot areas with poor 4G coverage) | N offline capabilities SLA undefined; Retry queue max items; Conflict resolution strategy offline edit vs concurrent online edit = UNDEFINED | IDN-FE-001 scope offline listed PART 22; Activity A1 training attendance critical use case for offline recording; Indexed DB schema outline shell delivery SPRINT 0 has no schema (not implementation code — architecture spec level) | Conflict rule: offline submit Coach marks present vs online Admin already marked absent — which wins LWW? timestamp? actor priority? Coach role > OrgAdmin? Need explicit rule; Attendance Offline max age 30 days submission accepted? If older maybe out of ActivityPolicy 90 day window anyway | Conflict attendance wrong → player training count wrong → ActivityPolicy min_events=2 not achieved → incorrectly dropped from ACTIVE state TEMPORARILY_INACTIVE incorrectly → VAP count wrong | **CRITICAL** (VAP counting correctness rural pilot areas low signal = main population Phase 0) | Principal FE Architect Principal Football Operations Expert (attendance business rule conflict) | Conflict resolution rule: attendance Present wins over Absent (safety: better accidentally count present then miss activity); Business rule must be APPROVED Football Operations expert not invented; Max offline submission window days = Council policy value NOT INVENTED 7d default; Offline queue size browser threshold MB; Indexed DB schema spec; Queue sync order preserved oldest first; Conflict log audit event written when resolve not silent | (1) Conflict test: offline Present Coach at 10:00am; online Absent Admin at 10:05am; sync later → attendance final = Present (per rule) + event ACT-04 ConflictLogged audit; (2) Offline queue 2000 items; reconnection 2G slow → syncs all in order no loss; (3) Attendance submission age > X days COUNCIL → reject submission reason "Activity window passed, resubmit with evidence" | Conflict resolution rule PRESENT wins over ABSENT NOT APPROVED invented proposal only; Actual rule MUST BE Council Football Ops APPROVED NOT agent decision; Offline submission window days UNDEFINED COUNCIL |
| **PERF-01-F14** | Performance budget per screen type: (a) Landing public First Contentful Paint < budget; (b) Authenticated dashboard render < budget; (c) Player timeline (50 events + 200 events) < budget; (d) Consent flow 8 screens wizard; (e) Roster page 500 players; All 5 categories have NO approved budget in current spec | N ALL 5 budget categories UNDEFINED; IDN-DS-001 44px touch baseline visual not performance budget ms | IDN-FE-001 PERFORMANCE PART 20 (coverage AUD-IDN-2026-002) · Only visual 44px baseline defined DS philosophy; No ms budget; No Lighthouse score targets (performance/accessibility/best practice scores) | Cannot budget frontend CI/CD pipeline gate fails Lighthouse < X without target defined; Accessibility budget 100 already defined WCAG AA; Performance contrast 92+ color contrast defined already (different budget accessibility ok visual yes; ms no) | Slow dashboard → Guardian abandons consent mid flow → CTI drops | **MAJOR** | Principal FE Principal UX | Council approve 5-category performance budget + Lighthouse score threshold minimum; Add budget to PerformancePolicy named versioned; CI/CD gate PR build fail if budget exceeded; Synthetic monitoring always-on dashboards after Phase 0 | (1) Lighthouse CI all PRs > approved score threshold; (2) WebPageTest dashboard 5 categories weekly report < budget ms p50 p95 mobile; (3) Core Web Vitals CWV INP < 200ms (Google CWV INP metric 2024+ standard reference if council adopts standard Google; NOT adopt yet unless Council approve) | ALL 5 MS BUDGET CATEGORIES UNDEFINED COUNCIL; Lighthouse score threshold UNDEFINED; WCAG AA color contrast APPROVED DS visual requirement yes accessibility defined |
| **PERF-01-F15** | Observability: Traceability distributed (request → domain event → DB row → projection update); Metrics Prometheus names: vap_count, cti_pct, ndi_density, jcs_score; Alerts on phase halt CTI; Audit log volume per day Phase 3 scale; Error budget policy SLO availability %; Dashboard Grafana/Lovable equivalent panels list | N SLO availability, error budget %, MB log volume day = ALL UNDEFINED · 4 metric names APPROVED SPECIFIED VAP/CTI/NDI/JCS from glossary KPI triad = YES (source names approved glossary) | IDN-FE-001 Observability PART 22 (deep dive AUD-IDN-2026-002); glossary KPI metric names defined quality gates strategic KPI triad PART 01 G1 | Metrics named; SLO availability target Phase 0 99.9% vs 99.5% (difference cost); No error budget burn rate alert defined; No dashboards list panels mandatory; No distributed trace 100% sample vs 1% for cost decision | SLO not defined → availability dispute; down 4 hours Phase 0 registration week = incident severity debatable if no agreed target → operational confusion | **MAJOR** | Principal DevOps Architect | Council approve SLO availability target Phase 0/1/2/3/4 (ramp up maturity); Error budget 0.1% hours; Mandatory dashboard panels list: 4 headline KPI, 7 projection lag PERF-01-F08, 4 rate limit, VAP lifecycle state transition pie, p95 latency API, Core Web Vitals, audit log volume MB/day, cache hit% by category; Distributed trace sample cost/accuracy tradeoff decision | (1) SLA document signed Council availability % target per phase; (2) Dashboard JSON spec panels list matching 10 types above; (3) Synthetic test downtime sim 1 hour → alert fires within 2 min correct severity | Metric names APPROVED glossary 4 KPIs; SLO % target UNDEFINED COUNCIL |
| **PERF-01-F16** | Membership Secondary addition read path: Phase 0 10 SSB → avg 2 Secondary per Player = 3,000 rows; Phase 3 National 500k × 5 = 2.5M Secondary memberships plus 500k Primary = 3M rows total membership table; Indexing on person_ref, org_id, dates, type — specified ERD logical but index strategy size / partial index / query plan analyze not defined | Y (Membership Primary + Secondary cardinality APPROVED ADR-0003) · count pattern approved; INDEX plan UNDEFINED | ERD entity Membership; ADR-0003 INV-MEM cardinality Primary 1 Secondary 0..N; Query pattern: person's membership history (both types); Org roster queries Primary only default, Secondary camp views separate page | Partial index WHERE type = PRIMARY AND status = ACTIVE → small size (500k rows only Phase 3) not full table scan 3M; Status history soft deletes index; Query plan roster default uses partial index not full table | Coach roster page default full table 3M rows without partial index → 5 seconds load → abandoned registration at SSB → new players VAP never | **MAJOR** (roster load primary UX OrgAdmin) | Principal Data Architect (index) | Define index strategy appendix ERD; Partial indexes for common queries; EXPLAIN ANALYZE plan verified all 12 query types Q-01..12 uses indexes not seq scan; Index bloat monitoring VACUUM schedule; Roster default query Primary Active partial index only; Secondary Membership tab explicit navigation to not slow primary | (1) EXPLAIN Q-org-roster → partial index scan PRIMARY+ACTIVE rows; (2) 3M synthetic rows test → roster API p95 < budget ms Council; (3) AutoAnalyze / VACUUM schedule documented | Cardinality ADR-0003 APPROVED; Index type list plan UNDEFINED; ms roster budget UNDEFINED |

---

## Approved Policy Values Found (SUMMARY — all numbers traceable):

Only TWO performance-relevant numeric policies traced to APPROVED sources (no invented!):

| Policy | Value | Source Artifact (APPROVED / ACCEPTED) |
|---|---|---|
| ActivityPolicy rolling window days = 90; min_events = 2; min_distinct_dates = 2; adult_attestation = true; GLOBAL v1 default active | 90 days · 2 events · 2 dates · required attestation | PRG-MET-001 Active Football Activity counting rule APPROVED G0 6 Aug 2026 |
| CTI halt semantic: ANY floor value met or NOT → phase exit BLOCKED if CTI < floor REGARDLESS VAP count attained | Halt semantics enabled (no number — rule itself) | glossary.md §Consent Trust Index line 46 APPROVED ubiquitous language |

## UNDEFINED Performance Budget Values Count = 23 items requiring Council Policy decisions:

| Count | Category |
|---|---|
| 4 | Rate limit thresholds (F09) |
| 5 | Screen performance ms budget (F14) |
| 2 | Football ID lookup latency display_code / football_id (F01 + F02 resolver) |
| 2 | Projection: build SLA events/minute + lag alert minutes (F04 + F08) |
| 1 | Async export job SLA hours + chunk rows (F05) |
| 2 | Event Store partition strategy doc + cold archive years (F07) |
| 6 | Caching: TTL seconds 4 categories + invalidation map + non-cacheable list already defined |
| 1 | INV-18 Eligibility check window hours before close (F03) |
| 2 | Offline conflict resolution + offline submission window days (F13) |
| 2 | Lighthouse score minimum + SLO availability % per phase (F14 + F15) |

Total UNDEFINED = 23 distinct performance policy values requiring formal EFPEC Council Performance Policy v1 approval. Critical note: none invented defaults anywhere in Sprint 3 output; maintained explicitly OPEN until Council decides.

---

## Severity Summary

| Severity | Count | IDs |
|---|---|---|
| BLOCKER | 1 | PERF-01-F10 Caching consent status realtime NEVER CACHE rule violation (if done wrong → breaks Rule 0 High-Risk Revocation immediate effect requirement) |
| CRITICAL | 5 | F03 Eligibility window INV-18; F05 National roster 500k export async; F07 Event store partition undefined P4 scale impossible; F09 Rate Limit thresholds combined security+perf; F13 Offline Attendance Conflict Resolution rule VAP counting correctness rural |
| MAJOR | 8 | F01 lookup; F04 projection; F06 replica; F08 projection lag; F11 cache invalidation map (missing mapping 22cmd→keys); F12 Opaque FID structural positive risk MINOR; F14 frontend ms budgets; F15 SLO observability targets; F16 indexes |
| MINOR | 1 | F02 resolver overhead + F12 Opaque architecture positive prevention → minor |
| OBSERVATION | 0 | — |

---

*Document prepared 9 Aug 2026 Sprint 3 PERF-01 mandate. NO performance budgets, SLA percentages, rate-limit threshold numbers, cache TTL seconds, offline window days values were INVENTED by agent. All 23 undefined values explicitly flagged UNDEFINED COUNCIL OWNED requiring PerformancePolicy v1 Council decision. Only 2 policies traced back to APPROVED artifacts (PRG-MET-001 90/2/2; glossary CTI halt semantic rule). Pattern-level architecture decisions (INV-18, opaque FID, forward-only migration, strict CQRS, non-cacheable list) were correctly SPECIFIED traced to existing approved documents. No load testing performed because G6 BLOCKED per lifecycle.*
